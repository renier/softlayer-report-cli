require 'spec_helper'

describe Softlayer::Cli do
  it 'has a version number' do
    expect(Softlayer::Cli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
